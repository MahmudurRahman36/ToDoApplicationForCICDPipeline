services:
  # React App served via Nginx
  # Note: This is a pure frontend app — tasks are stored in browser localStorage.
  # No backend/database connection is required.
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo_app
    ports:
      - "80:80"
    restart: always
